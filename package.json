{
  "name": "@quartz/styles",
  "description": "Quartz style dictionary",
  "version": "0.2.1",
  "devDependencies": {
    "@quartz/stylelint-config": "^1.2.0",
    "style-dictionary": "^2.10.0",
    "stylelint": "^13.6.1"
  },
  "main": "scss/tokens.scss",
  "sass": "scss/tokens.scss",
  "files": [
    "scss/**/*.scss",
    "dictionaries/colors.json"
  ],
  "scripts": {
    "build": "node build.js",
    "test": "stylelint 'scss/**/*.scss'"
  },
  "dependencies": {
    "normalize.css": "^8.0.1"
  }
}
